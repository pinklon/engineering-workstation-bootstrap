name: validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install validation dependency
        run: brew install shellcheck
      - name: Validate repository
        run: bash validation/validate.sh
