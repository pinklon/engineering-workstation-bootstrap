#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MODE="${1:-install}"
PROFILE="${WORKSTATION_PROFILE:-mac-studio}"
CONFIG_ROOT="${HOME}/.config/engineering-workstation-bootstrap"
TOOL_ROOT="${HOME}/.local/share/codex-tools"
RECEIPT_ROOT="${HOME}/.local/state/engineering-workstation-bootstrap/receipts"

log() { printf '%s\n' "$*"; }
die() { printf 'ERROR: %s\n' "$*" >&2; exit 1; }
need_human() { printf '\nACTION REQUIRED: %s\n' "$*"; }

install_xcode_tools() {
  xcode-select -p >/dev/null 2>&1 && return 0
  need_human "Install Xcode Command Line Tools in the macOS dialog, then rerun this command."
  xcode-select --install || true
  exit 10
}

install_homebrew() {
  command -v brew >/dev/null 2>&1 && return 0
  need_human "Homebrew requires an administrator-approved installation."
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  if [[ -x /opt/homebrew/bin/brew ]]; then eval "$(/opt/homebrew/bin/brew shellenv)"; fi
  command -v brew >/dev/null 2>&1 || die "Homebrew installation did not become available."
}

install_packages() {
  log "Installing declared host packages..."
  brew bundle --file "$ROOT/Brewfile"
  command -v mise >/dev/null 2>&1 && mise install --cd "$ROOT"
  npm install --global wrangler
}

managed_include() {
  local target="$1" line="$2"
  touch "$target"
  grep -Fqx "$line" "$target" 2>/dev/null || printf '\n%s\n' "$line" >> "$target"
}

configure_host() {
  mkdir -p "$HOME/bin" "$CONFIG_ROOT" "$TOOL_ROOT" "$RECEIPT_ROOT"
  cp "$ROOT/config/shell.zsh" "$CONFIG_ROOT/shell.zsh"
  cp "$ROOT/config/gitconfig" "$CONFIG_ROOT/gitconfig"
  cp "$ROOT/config/ssh-config" "$CONFIG_ROOT/ssh-config"
  managed_include "$HOME/.zshrc" 'source "$HOME/.config/engineering-workstation-bootstrap/shell.zsh"'
  managed_include "$HOME/.gitconfig" '[include]'
  managed_include "$HOME/.gitconfig" '    path = ~/.config/engineering-workstation-bootstrap/gitconfig'
  mkdir -p "$HOME/.ssh"; chmod 700 "$HOME/.ssh"
  managed_include "$HOME/.ssh/config" 'Include ~/.config/engineering-workstation-bootstrap/ssh-config'
  chmod 600 "$HOME/.ssh/config"
  for tool in workstation-bootstrap workstation-doctor github-auth-doctor cloudflare-auth-doctor browser-gate-python; do
    ln -sfn "$ROOT/bin/$tool" "$HOME/bin/$tool"
  done
}

enroll_github() {
  if ! gh auth status --hostname github.com >/dev/null 2>&1; then
    need_human "Complete GitHub browser OAuth and MFA."
    gh auth login --hostname github.com --git-protocol ssh --web
  fi
  if ! ssh -G git@github.com >/dev/null 2>&1; then die "SSH client configuration is invalid."; fi
  if ! find "$HOME/.ssh" -maxdepth 1 -name '*.pub' -print -quit | grep -q .; then
    local key="$HOME/.ssh/id_ed25519_github"
    need_human "Create the GitHub SSH key. You may accept the default passphrase prompt."
    ssh-keygen -t ed25519 -f "$key" -C "$(gh api user --jq .login)@$(scutil --get ComputerName 2>/dev/null || hostname)"
    gh ssh-key add "$key.pub" --title "$(scutil --get ComputerName 2>/dev/null || hostname)-$(date +%Y-%m-%d)"
  fi
  gh auth setup-git
}

enroll_cloudflare() {
  if ! wrangler whoami >/dev/null 2>&1; then
    need_human "Complete Cloudflare browser OAuth and MFA. No deployment will occur."
    wrangler login
  fi
}

configure_browser_gate() {
  "$ROOT/bootstrap/configure-browser-gate.sh"
}

clone_repositories() {
  "$ROOT/bootstrap/clone-repositories.sh" "$PROFILE"
}

case "$MODE" in
  install)
    [[ "$(uname -s)" == "Darwin" ]] || die "install currently supports macOS."
    install_xcode_tools
    install_homebrew
    install_packages
    configure_host
    enroll_github
    enroll_cloudflare
    configure_browser_gate
    clone_repositories
    "$ROOT/bin/workstation-doctor" --full
    ;;
  reconcile)
    install_homebrew
    install_packages
    configure_host
    configure_browser_gate
    clone_repositories
    "$ROOT/bin/workstation-doctor" --full
    ;;
  status) exec "$ROOT/bin/workstation-doctor" --full ;;
  dry-run)
    log "Profile: $PROFILE"
    log "Would install Brewfile and mise.toml dependencies, configure managed fragments, enroll providers, configure browser gate, clone declared repositories, and validate."
    ;;
  *) die "Usage: workstation-bootstrap {install|reconcile|status|dry-run}" ;;
esac
