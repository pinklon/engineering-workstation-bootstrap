schema_version: 2
classes: [non-secret, discovered, secret-bound, oauth-enrolled, brokered, optional, retired]
bindings:
  PATH: {class: non-secret, origin: shell-startup}
  WORKSTATION_PROFILE: {class: optional, origin: operator}
  WORKSTATION_STAGE_ROOT: {class: non-secret, origin: operator}
  WORKSTATION_ACTIVATION_CONTRACT: {class: non-secret, origin: operator}
  WORKSTATION_PRIVATE_PROFILE: {class: discovered, origin: local-generated-overlay}
  WORKSTATION_DRIVE_ROOT: {class: discovered, origin: mount-discovery}
  GH_TOKEN: {class: secret-bound, origin: external, persist: false}
  GITHUB_TOKEN: {class: secret-bound, origin: external, persist: false}
  OPENAI_API_KEY: {class: secret-bound, origin: external, persist: false}
  SUPABASE_ACCESS_TOKEN: {class: oauth-enrolled, origin: provider, persist: false}
  CLOUDFLARE_API_TOKEN: {class: secret-bound, origin: provider, persist: false}
generation:
  include_values_for_classes: [non-secret, discovered]
  exclude_values_for_classes: [secret-bound, oauth-enrolled, brokered]
  receipts_contain_values: false
